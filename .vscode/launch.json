{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C Debug (Windows)",
            "type": "cppvsdbg",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}